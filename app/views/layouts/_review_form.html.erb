<%= render "layouts/errors_reviews", :object => @review %>

<%= form_for [@product, @review] do |f| %>
  <%= f.label :content_body %>
  <%= f.text_area :content_body %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <p><%= f.submit %></p>
<% end %>
